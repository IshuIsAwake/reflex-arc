# Source critiques live in docs/critique/. The novelty review is tracked; the full
# review is not, because it judges an execution plan that does not exist yet.
*.docx
*.doc
*.pdf
!docs/critique/gemini-novelty-critique.docx

# Context for a model, not for a teammate. Everyone keeps their own.
HANDOFF.md
.claude/

# CLAUDE.md is the tracked one and holds the repo rules whatever tool you drive.
# Anything else is that tool's copy of them and drifts the moment it is edited.
GEMINI.md
AGENTS.md
.cursorrules

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python (for when code lands)
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Experiment artifacts
runs/
wandb/
checkpoints/
*.ckpt
*.pt
